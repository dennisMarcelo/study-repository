aqui eu utilizeio o:
 -'express'     para criar um servidor na minha maquina;
 -'sequelize'   para se conectar ao banco de dados;
 -'hendlebars'  para criar template engines;
 -'body-parse'  para receber dados do formulário;
function getAverage(a, b, c) {
  let result = (a*2 + b*3 + c*5) / (2+3+5)

  console.log(`MEDIA = ${result.toFixed(2)}`)
}



getAverage(5.0, 6.0, 7.0);
getAverage(5.0, 10.0, 10.0);
getAverage(10.0, 10.0, 5.0);
